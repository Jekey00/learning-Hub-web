class SupabaseConfig {
  // Diese Werte dienen als Fallback für die lokale Entwicklung.
  // Überschreibe sie lokal oder nutze --dart-define beim Build.
  // ACHTUNG: Diese Datei wird auf GitHub hochgeladen. KEINE ECHTEN KEYS HIER!
  static const String supabaseUrl = 'https://YOUR_PROJECT_ID.supabase.co';
  static const String supabaseAnonKey = 'YOUR_ANON_PUBLIC_KEY';
}
